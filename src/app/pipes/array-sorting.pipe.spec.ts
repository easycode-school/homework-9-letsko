import { ArraySortingPipe } from './array-sorting.pipe';

describe('ArraySortingPipe', () => {
  it('create an instance', () => {
    const pipe = new ArraySortingPipe();
    expect(pipe).toBeTruthy();
  });
});
